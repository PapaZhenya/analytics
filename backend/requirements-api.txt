-r requirements-base.txt
email-validator==2.2.0
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
python-multipart==0.0.19
slowapi==0.1.9

# Test tooling (kept in the api image's requirements so CI can run the full suite
# without pulling the heavy worker requirements just to exercise the API layer)
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
